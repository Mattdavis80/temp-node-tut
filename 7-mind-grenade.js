const num1 = 5;
const num2 = 10;

function addValues() {
  console.log(`the sum is ${num1 + num2}`);
}

addValues();

// When you require a module, you invoke its function. Without assigning the module to a variable, it is called when required in the order they are written
